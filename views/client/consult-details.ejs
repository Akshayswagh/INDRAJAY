<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('../partials/head'); %>

    <style>
      .page-body {
        font-family: "Roboto", "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        line-height: 1.7;
        color: #2c3e50;
        background-color: #f4f7f6;
        overflow-x: hidden;
        padding: 50px 20px;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        align-items: center;
        justify-content: center;
      }

      @keyframes fadeInScaleUp {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      @keyframes slideInFromTop {
        from {
          opacity: 0;
          transform: translateY(-30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes slideInFromRight {
        from {
          opacity: 0;
          transform: translateX(40px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes slideInContent {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .consultation-detail-card {
        background-color: #ffffff;
        padding: 40px 50px;
        border-radius: 15px;
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
        max-width: 900px;
        width: 100%;
        position: relative;
        animation: fadeInScaleUp 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94)
          forwards;
        margin: 20px 0;
      }

      .enquiry-cta-container {
        position: absolute;
        top: 30px;
        right: 40px;
        z-index: 10;
        opacity: 0;
        animation: slideInFromRight 0.7s ease-out 0.5s forwards;
      }

      .enquiry-cta-button {
        display: inline-block;
        background-color: #f39c12;
        color: #ffffff;
        padding: 12px 28px;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        font-size: 1rem;
        transition: background-color 0.3s ease, transform 0.3s ease,
          box-shadow 0.3s ease;
        box-shadow: 0 4px 12px rgba(243, 156, 18, 0.35);
      }

      .enquiry-cta-button:hover {
        background-color: #e67e22;
        transform: translateY(-3px) scale(1.03);
        box-shadow: 0 6px 16px rgba(243, 156, 18, 0.45);
      }

      .consultation-main-title {
        font-size: clamp(2rem, 5vw, 2.7rem);
        color: #00796b;
        margin-bottom: 15px;
        font-weight: 700;
        line-height: 1.3;
        text-align: left;
        opacity: 0;
        animation: slideInFromTop 0.7s ease-out 0.2s forwards;
        padding-bottom: 20px;
        border-bottom: 2px solid #00796b;
        display: inline-block;
      }

      .consultation-meta-info {
        font-size: 0.9rem;
        color: #5a6a7a;
        margin-bottom: 30px;
        opacity: 0;
        animation: slideInFromTop 0.7s ease-out 0.35s forwards;
      }

      .consultation-meta-info span {
        margin-right: 15px;
      }

      .consultation-description-content {
        font-size: 1.05rem;
        color: #5a6a7a;
        line-height: 1.8;
        opacity: 0;
        animation: slideInContent 0.8s ease-out 0.6s forwards;
        margin-top: 20px;
      }

      .consultation-description-content p {
        margin-bottom: 1.5em;
      }

      .consultation-description-content h3 {
        color: #2c3e50;
        font-size: 1.4rem;
        margin-top: 35px;
        margin-bottom: 18px;
        font-weight: 600;
        border-left: 4px solid #f39c12;
        padding-left: 12px;
      }

      .consultation-description-content ul {
        list-style-type: none;
        padding-left: 0;
        margin-bottom: 25px;
      }

      .consultation-description-content li {
        background-color: transparent;
        padding: 15px 20px;
        margin-bottom: 10px;
        border-radius: 8px;
        font-size: 1rem;
        color: #2c3e50;
        position: relative;
        padding-left: 35px;
        border: 1px solid #e0e0e0;
        transition: background-color 0.3s ease, border-color 0.3s ease,
          transform 0.3s ease;
      }

      .consultation-description-content li:before {
        content: "✓";
        color: #00796b;
        font-weight: bold;
        position: absolute;
        left: 12px;
        top: 14px;
        font-size: 1.1rem;
      }

      .consultation-description-content li:hover {
        background-color: #e0f2f1;
        border-color: #00796b;
        transform: translateX(5px);
      }

      .navigation-back-link {
        margin-top: 45px;
        text-align: center;
        opacity: 0;
        animation: slideInContent 0.7s ease-out 1s forwards;
      }

      .navigation-back-link a {
        color: #00796b;
        text-decoration: none;
        font-weight: 600;
        font-size: 1.05rem;
        padding: 12px 25px;
        border: 2px solid #00796b;
        border-radius: 30px;
        transition: background-color 0.3s ease, color 0.3s ease,
          transform 0.2s ease;
        display: inline-block;
      }

      .navigation-back-link a:hover {
        background-color: #00796b;
        color: #ffffff;
        transform: translateY(-2px);
      }

      @media (max-width: 480px) {
        .consultation-main-title {
          font-size: 1.6rem;
          display: block;
          text-align: center;
          margin-bottom: 25px;
        }

        .enquiry-cta-container {
          position: relative;
          text-align: center;
          margin-bottom: 25px;
          top: auto;
          right: auto;
          animation: fadeInScaleUp 0.7s ease-out 0.5s forwards;
        }

        .consultation-detail-card {
          padding: 25px 15px;
        }

        .consultation-meta-info {
          text-align: center;
        }

        .consultation-description-content h3 {
          font-size: 1.15rem;
        }

        .consultation-description-content li {
          font-size: 0.9rem;
        }
      }
    </style>
  </head>
  <body>
    <%- include('../partials/navbar'); %>

    <div class="page-body">
      <div class="consultation-detail-card">
        <div class="enquiry-cta-container">
          <a href="/enquiry" class="enquiry-cta-button" id="enquiryBtn">Enquire Now</a>
        </div>

        <h1 class="consultation-main-title" id="consultationName">
          <%- consultation.title %>
        </h1>
        <br />

        <div class="consultation-description-content" id="consultationDetails">
          <p style="text-align: justify"><%- consultation.description %></p>
        </div>

        <div class="navigation-back-link">
          <a href="/consultation" class="back-link"
            >← Explore Other Consultations</a
          >
        </div>
      </div>
    </div>

    <%- include('../partials/footer'); %> <%- include('../partials/scripts'); %>
  </body>
</html>
