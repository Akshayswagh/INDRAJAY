<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="/css/pulses.css" />
    <%- include('../partials/head'); %>
  </head>
  <body>
    <%- include('../partials/navbar'); %>

    <section class="pulses">
      <div class="page-container">
        <div class="page-heading">
          <h1>Pulses</h1>
          <p>
            Discover a rich variety of high-quality pulses packed with protein
            and essential nutrients—perfect for a healthy and balanced diet.
          </p>
        </div>

        <div class="products-container">
          <% if (pulses.length > 0) { %> <% pulses.forEach(oilseed => { %>

          <!-- Product 1 -->
          <a href="/exports/view/<%= pulse._id %>" class="product-link">
            <div class="product-card">
              <div class="product-image-container">
                <img
                  src="<%= pulse.image %>"
                  alt="<%= pulse.name %>"
                  class="product-image"
                />
              </div>
              <div class="product-info">
                <h3 class="product-name"><%= pulse.name %></h3>
                <span class="product-category"><%= pulse.category %></span>
              </div>
            </div>
          </a>

          <% }) %> <% } else { %>
          <p>No products Available in this category.</p>
          <% } %>
        </div>
        <!-- <div class="view-more">
          <button class="view-more-btn">View More Products</button>
        </div> -->
      </div>
    </section>
    <%- include('../partials/footer'); %> <%- include('../partials/scripts'); %>
  </body>
</html>
