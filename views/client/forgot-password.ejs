<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
  <title>Forgot Password</title>
  <style>
    :root {
      --fp-primary-color: #f47321;
      --fp-primary-hover: #e0661b;
      --fp-text-color: #333;
      --fp-muted-text: #6c757d;
      --fp-success-color: #28a745;
      --fp-border-color: #ced4da;
      --fp-error-color: #dc3545;
      --fp-background: #f8f9fa;
      --fp-white: #ffffff;
      --fp-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .fp-body {
      background-color: var(--fp-background);
      color: var(--fp-text-color);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .fp-login-container {
      max-width: 480px;
      width: 100%;
      margin: auto;
      padding: 20px;
      flex: 1;
      display: flex;
      align-items: center;
    }

    .fp-login-card {
      background: var(--fp-white);
      border-radius: 10px;
      box-shadow: var(--fp-shadow);
      padding: 2.5rem;
      width: 100%;
      text-align: center;
    }

    .fp-login-header {
      margin-bottom: 2rem;
    }

    .fp-login-icon {
      width: 80px;
      height: 80px;
      margin: 0 auto 1rem;
      background-color: rgba(244, 115, 33, 0.1);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .fp-login-icon svg {
      width: 36px;
      height: 36px;
      color: var(--fp-primary-color);
    }

    .fp-login-title {
      font-size: 1.75rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--fp-text-color);
    }

    .fp-login-subtitle {
      color: var(--fp-muted-text);
      font-size: 1rem;
      margin-bottom: 1.5rem;
    }

    .fp-form-group {
      margin-bottom: 1.5rem;
      text-align: left;
      position: relative;
    }

    .fp-form-input {
      width: 100%;
      padding: 1rem;
      border: 1px solid var(--fp-border-color);
      border-radius: 6px;
      font-size: 1rem;
      transition: all 0.2s ease;
      background-color: transparent;
    }

    .fp-form-input:focus {
      outline: none;
      border-color: var(--fp-primary-color);
      box-shadow: 0 0 0 3px rgba(244, 115, 33, 0.2);
    }

    .fp-form-label {
      position: absolute;
      left: 1rem;
      top: 1rem;
      color: var(--fp-muted-text);
      transition: all 0.2s ease;
      pointer-events: none;
      background-color: var(--fp-white);
      padding: 0 0.25rem;
    }

    .fp-form-input:focus + .fp-form-label,
    .fp-form-input:not(:placeholder-shown) + .fp-form-label {
      top: -0.6rem;
      font-size: 0.75rem;
      color: var(--fp-primary-color);
    }

    .fp-submit-button {
      width: 100%;
      padding: 1rem;
      background-color: var(--fp-primary-color);
      color: var(--fp-white);
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.2s ease;
      margin-top: 0.5rem;
    }

    .fp-submit-button:hover {
      background-color: var(--fp-primary-hover);
    }

    .fp-submit-button:disabled {
      background-color: var(--fp-muted-text);
      cursor: not-allowed;
    }

    .fp-login-footer {
      margin-top: 1.5rem;
      font-size: 0.875rem;
      color: var(--fp-muted-text);
    }

    .fp-footer-link {
      color: var(--fp-primary-color);
      text-decoration: none;
      font-weight: 600;
    }

    .fp-footer-link:hover {
      text-decoration: underline;
    }

    .fp-message-box {
      padding: 1rem;
      border-radius: 6px;
      margin-top: 1.5rem;
      font-size: 0.875rem;
      display: none;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }

    .fp-error-message {
      background-color: rgba(220, 53, 69, 0.1);
      color: var(--fp-error-color);
      border: 1px solid rgba(220, 53, 69, 0.2);
    }

    .fp-success-message {
      background-color: rgba(40, 167, 69, 0.1);
      color: var(--fp-success-color);
      border: 1px solid rgba(40, 167, 69, 0.2);
    }

    .fp-loading {
      position: relative;
      pointer-events: none;
      color: transparent;
    }

    .fp-loading::after {
      content: "";
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      width: 20px;
      height: 20px;
      border: 2px solid rgba(255, 255, 255, 0.3);
      border-radius: 50%;
      border-top-color: white;
      animation: fp-spin 1s linear infinite;
    }

    @keyframes fp-spin {
      to { transform: translate(-50%, -50%) rotate(360deg); }
    }

    @media (max-width: 480px) {
      .fp-login-container {
        padding: 1rem;
      }
      
      .fp-login-card {
        padding: 1.5rem;
      }
      
      .fp-login-icon {
        width: 72px;
        height: 72px;
      }
    }
  </style>
</head>
<body class="fp-body">
  <%- include('../partials/navbar'); %>

  <div class="fp-login-container">
    <div class="fp-login-card">
      <div class="fp-login-header">
        <div class="fp-login-icon">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path>
          </svg>
        </div>
        <h1 class="fp-login-title">Reset Password</h1>
        <p class="fp-login-subtitle">Enter your email to receive a reset link</p>
      </div>

      <form id="forgotPasswordForm">
        <div class="fp-form-group">
          <input
            type="email"
            id="email"
            name="email"
            class="fp-form-input"
            placeholder=" "
            required
          />
          <label for="email" class="fp-form-label">Email address</label>
        </div>

        <button type="submit" class="fp-submit-button" id="submitButton">
          Send Reset Link
        </button>
      </form>

      <div class="fp-login-footer">
        <p>
          Remember your password?
          <a href="/alogin" class="fp-footer-link">Login</a>
        </p>
      </div>

      <div id="responseMessage" class="fp-message-box" role="alert"></div>
    </div>
  </div>

  <%- include('../partials/footer'); %>
  <%- include('../partials/scripts'); %>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.getElementById('forgotPasswordForm');
      const submitButton = document.getElementById('submitButton');
      const responseMessage = document.getElementById('responseMessage');

      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const email = form.email.value.trim();
        
        // Basic email validation
        if (!email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
          responseMessage.innerHTML = `
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="12" y1="8" x2="12" y2="12"></line>
              <line x1="12" y1="16" x2="12.01" y2="16"></line>
            </svg>
            Please enter a valid email address
          `;
          responseMessage.className = 'fp-message-box fp-error-message';
          responseMessage.style.display = 'flex';
          return;
        }

        try {
          // Show loading state
          submitButton.disabled = true;
          submitButton.classList.add('fp-loading');
          responseMessage.style.display = 'none';

          const response = await fetch('/api/auth/forgot-password', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({ email }),
          });

          const data = await response.json();
          
          if (response.ok) {
            responseMessage.innerHTML = `
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                <polyline points="22 4 12 14.01 9 11.01"></polyline>
              </svg>
              ${data.message || 'Password reset link sent successfully!'}
            `;
            responseMessage.className = 'fp-message-box fp-success-message';
          } else {
            responseMessage.innerHTML = `
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <line x1="12" y1="8" x2="12" y2="12"></line>
                <line x1="12" y1="16" x2="12.01" y2="16"></line>
              </svg>
              ${data.message || 'Error sending reset link'}
            `;
            responseMessage.className = 'fp-message-box fp-error-message';
          }
          responseMessage.style.display = 'flex';
        } catch (error) {
          console.error('Error:', error);
          responseMessage.innerHTML = `
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="12" y1="8" x2="12" y2="12"></line>
              <line x1="12" y1="16" x2="12.01" y2="16"></line>
            </svg>
            Network error. Please try again later.
          `;
          responseMessage.className = 'fp-message-box fp-error-message';
          responseMessage.style.display = 'flex';
        } finally {
          submitButton.disabled = false;
          submitButton.classList.remove('fp-loading');
        }
      });
    });
  </script>
</body>
</html>