<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Industrial Services - Min 1rem Fonts</title>
    <%- include('../partials/head'); %>

    <style>
      body {
        font-family: "Roboto", "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        line-height: 1.7;
        color: #2c3e50; /* text-dark */
        background-color: #f8f9fa; /* bg-light */
        overflow-x: hidden;
      }

      /* --- Keyframe Animations (Reused) --- */
      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
      @keyframes slideInFromBottom {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      @keyframes underlineGrowPrimary {
        from {
          width: 0;
        }
        to {
          width: 70px;
        }
      }

      /* Container */
      .industrial-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
      }

      /* Hero Section */
      .industrial-hero {
        padding: 70px 0 50px;
        text-align: center;
        background: linear-gradient(
            rgba(255, 255, 255, 0.9),
            rgba(255, 255, 255, 0.9)
          ),
          url("your-industrial-background-image.jpg") center center/cover
            no-repeat;
        background-color: #ffffff; /* bg-white */
        border-bottom: 1px solid #e0e0e0;
        animation: fadeIn 0.9s ease-out;
      }

      .industrial-hero h1 {
        font-size: clamp(2rem, 4.8vw, 3.2rem);
        color: #34495e; /* accent-industrial */
        margin-bottom: 15px;
        font-weight: 700;
        opacity: 0;
        animation: slideInFromBottom 0.7s ease-out 0.2s forwards;
      }

      .industrial-hero .tagline {
        font-size: clamp(1rem, 2.2vw, 1.15rem);
        color: #5a6a7a; /* text-light */
        max-width: 750px;
        margin: 0 auto 25px auto;
        line-height: 1.7;
        opacity: 0;
        animation: slideInFromBottom 0.7s ease-out 0.4s forwards;
      }

      .hero-cta-button {
        display: inline-block;
        background-color: #f39c12; /* secondary-color */
        color: #ffffff; /* bg-white */
        padding: 10px 28px;
        border-radius: 5px;
        text-decoration: none;
        font-weight: 600;
        font-size: 1rem;
        transition: background-color 0.3s ease, transform 0.3s ease;
        opacity: 0;
        animation: slideInFromBottom 0.7s ease-out 0.6s forwards;
        box-shadow: 0 3px 8px rgba(52, 73, 94, 0.15);
      }

      .hero-cta-button:hover {
        background-color: #e67e22;
        transform: translateY(-2px);
      }

      /* Services Section */
      .industrial-services-section {
        padding: 60px 0;
      }

      .industrial-services-section .section-title {
        text-align: center;
        font-size: clamp(1.8rem, 4vw, 2.6rem);
        color: #2c3e50; /* text-dark */
        margin-bottom: 15px;
        font-weight: 700;
        opacity: 0;
        animation: slideInFromBottom 0.6s ease-out 0.2s forwards;
        position: relative;
      }

      .industrial-services-section .section-title::after {
        content: "";
        display: block;
        width: 0;
        height: 3px;
        background-color: #34495e; /* accent-industrial */
        margin: 12px auto 0;
        border-radius: 2px;
        animation: underlineGrowPrimary 0.7s ease-out 0.8s forwards;
      }

      .industrial-services-section .section-subtitle {
        text-align: center;
        font-size: clamp(1rem, 1.8vw, 1.05rem);
        color: #5a6a7a; /* text-light */
        max-width: 650px;
        margin: 0 auto 40px auto;
        opacity: 0;
        animation: slideInFromBottom 0.6s ease-out 0.4s forwards;
      }

      .industrial-grid {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: center;
      }

      .industrial-card {
        background-color: #ffffff; /* bg-white */
        border-radius: 6px;
        padding: 20px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.07); /* card-shadow */
        transition: transform 0.25s ease, box-shadow 0.25s ease,
          border-left-color 0.25s ease;
        width: calc(33.333% - 13.33px);
        min-width: 270px;
        display: flex;
        flex-direction: column;
        opacity: 0;
        animation: slideInFromBottom 0.5s ease-out forwards;
        border-left: 4px solid transparent;
      }

      /* Stagger card animations */
      /* (same delays as your original CSS) */

      .industrial-card:nth-child(1) {
        animation-delay: 0.7s;
      }
      .industrial-card:nth-child(2) {
        animation-delay: 0.75s;
      }
      /* ... continue for all nth-child as in your original CSS ... */

      .industrial-card:hover {
        transform: translateY(-4px) scale(1.005);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1); /* card-hover-shadow */
        border-left-color: #f39c12; /* secondary-color */
      }

      .industrial-card h3 {
        font-size: 1.5rem;
        color: #00796B; /* accent-industrial */
        margin-bottom: 15px;
        font-weight: 600;
      }

      .industrial-card p {
        color: #5a6a7a; /* text-light */
        font-size: 1rem;
        flex-grow: 1;
        margin-bottom: 12px;
        line-height: 1.6;
      }

      .industrial-card .details-link {
        color: #00796b; /* primary-color */
        text-decoration: none;
        font-weight: 600;
        align-self: flex-start;
        transition: color 0.3s ease;
        font-size: 1rem;
      }

      .industrial-card .details-link:hover {
        color: #f39c12; /* secondary-color */
      }

      /* Responsive Adjustments */
      @media (max-width: 1100px) {
        .industrial-card {
          width: calc(50% - 10px);
        }
      }

      @media (max-width: 768px) {
        .industrial-hero {
          padding: 50px 0 30px;
        }
        .industrial-services-section {
          padding: 40px 0;
        }
        .industrial-card {
          width: 100%;
          min-width: unset;
          animation-delay: 0.2s !important;
        }
        .industrial-services-section .section-title::after {
          animation-delay: 0.5s;
        }
      }

      @media (max-width: 480px) {
        .industrial-hero h1 {
          font-size: 1.8rem;
        }
        .industrial-hero .tagline {
          font-size: 1rem;
        }
        .hero-cta-button {
          font-size: 1rem;
          padding: 10px 25px;
        }
        .industrial-services-section .section-title {
          font-size: 1.6rem;
        }
        .industrial-services-section .section-subtitle {
          font-size: 1rem;
        }
        .industrial-card h3 {
          font-size: 1.15rem;
          min-height: 0;
        }
        .industrial-card p {
          font-size: 1rem;
        }
        .industrial-card .details-link {
          font-size: 1rem;
        }
        .industrial-grid {
          gap: 15px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Assume Navbar is above this -->
    <%- include('../partials/navbar'); %>

    <div class="industrial-hero">
      <div class="industrial-container">
        <h1>Comprehensive Industrial Services</h1>
        <p class="tagline">
          Powering your operations with expert solutions. We deliver
          reliability, efficiency, and safety across a wide spectrum of
          industrial needs.
        </p>
        <a href="#industrial-offerings" class="hero-cta-button"
          >Explore Our Capabilities</a
        >
      </div>
    </div>

    <section id="industrial-offerings" class="industrial-services-section">
      <div class="industrial-container">
        <h2 class="section-title">Our Industrial Capabilities</h2>
        <p class="section-subtitle">
          From project inception to ongoing maintenance, our skilled teams
          provide end-to-end support for your industrial infrastructure and
          processes.
        </p>

        <div class="industrial-grid">
          <% if (services.length > 0) { %> <% services.forEach(service => { %>
          <div class="industrial-card">
            <h3><%-service.title %></h3>
            <p style="text-align: justify">
              <%= service.description.substring(0, 130) %>...
            </p>

            <a
              href="/api/indservices/service/<%= service._id %>"
              class="details-link"
              >View Details →</a
            >
          </div>
          <% }); %> <% } else { %>
          <p>No services available at this time.</p>
          <% } %>
        </div>
      </div>
    </section>

    <%- include('../partials/footer'); %> <%- include('../partials/scripts'); %>
  </body>
</html>
