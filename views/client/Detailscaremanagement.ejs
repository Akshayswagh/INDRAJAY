<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>

    <!-- Google Fonts for better typography -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&family=Open+Sans:wght@400;600&display=swap"
      rel="stylesheet"
    />
    <style>
      
      /* Main container for the "Care" page */
      .care-page-container {
        font-family: "Open Sans", sans-serif;
        color: #333;
        background-color: #f8f9fa;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        width: 100%;
        max-width: 100%;
        margin: 0 auto;
      }

      /* Hero Banner for Title */
      .care-hero-banner {
        padding: 40px 20px;
        text-align: center;
        /* margin-bottom: 30px; */ /* This was commented out, kept as is for now */
      }

      .care-hero-banner h1 {
        font-family: "Montserrat", sans-serif;
        font-size: 2.5em;
        color: #2c3e50;
        font-weight: 700;
        line-height: 1.3;
      }

      /* Main Image Display Section */
      .care-image-display-section {
        width: 100%;
        padding: 0 20px;
        margin-bottom: 30px;
        text-align: center;
      }

      .care-image-display-section img {
        display: inline-block;
        max-width: 100%;
        width: auto;
        height: auto;
        max-height: 65vh;
        object-fit: contain;
        border-radius: 8px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      }

      /* Content Details Section (Description & Button) */
      .care-content-details {
        padding: 0 20px 40px;
        max-width: 800px;
        margin: 0 auto;
      }

      .care-description-text {
        font-size: 1.1em;
        color: #495057;
        margin-bottom: 30px;
        text-align: justify;
        line-height: 1.8;
      }

      .care-description-text p:not(:last-child) {
        margin-bottom: 15px;
      }

      .care-enquire-button-wrapper {
        text-align: center;
        margin-top: 20px;
      }

      .care-enquire-btn {
        display: inline-block;
        background-color: #007bff;
        color: white;
        padding: 10px 28px;
        border-radius: 25px;
        font-family: "Montserrat", sans-serif;
        font-size: 0.95em;
        font-weight: 600;
        text-decoration: none;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        border: none;
      }

      .care-enquire-btn:hover {
        background-color: #0056b3;
        transform: translateY(-2px);
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
      }

      /* Responsive adjustments */
      @media (max-width: 768px) {
        .care-hero-banner h1 {
          font-size: 2em;
        }
        .care-image-display-section img {
          max-height: 55vh;
        }
        .care-description-text {
          font-size: 1em;
        }
        .care-enquire-btn {
          padding: 9px 25px;
          font-size: 0.9em;
        }
      }

      @media (max-width: 480px) {
        .care-hero-banner {
          padding: 30px 15px;
          /* margin-bottom: 20px; */ /* This was related to a commented out style */
        }
        .care-hero-banner h1 {
          font-size: 1.7em;
        }
        .care-image-display-section {
          padding: 0 15px;
          margin-bottom: 20px;
        }
        .care-image-display-section img {
          max-height: 45vh;
        }
        .care-content-details {
          padding: 0 15px 30px;
        }
        .care-description-text {
          font-size: 0.95em;
        }
        .care-enquire-btn {
          padding: 8px 22px;
          font-size: 0.85em;
        }
      }
    </style>
  </head>
  <body>
    <%- include('../partials/navbar'); %>
    <main class="care-page-container">
      <section class="care-hero-banner">
        <h1><%= care.title %></h1>
      </section>

      <section class="care-image-display-section">
        <img src="<%= care.image %>" alt="<%= care.title %>" />
      </section>

      <section class="care-content-details">
        <div class="care-description-text">
          <p><%= care.description %></p>
        </div>
        <div class="care-enquire-button-wrapper">
          <a href="/enquiry" class="care-enquire-btn">Enquire Now</a>
        </div>
      </section>
    </main>
    <%- include('../partials/footer'); %> <%- include('../partials/scripts'); %>
  </body>
</html>
