<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detailed Enquiry Form</title>
    <%- include('../partials/head'); %>

    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <!-- Optional: Font for the new header title if different -->
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@700&display=swap" rel="stylesheet">

    <style>
      /* Styles for the NEW Page Header Section (remaining parts) */
      .ph-title-section {
        text-align: center;
        margin-bottom: 30px;
      }

      .ph-main-title {
        font-family: 'Merriweather', serif; /* Example serif font */
        color: #386641; /* Dark Green */
        font-size: 2em; /* Adjust as needed */
        font-weight: 700;
        margin-bottom: 20px;
      }

      .ph-decorative-divider {
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto;
        max-width: 400px; /* Adjust width of the divider element */
      }

      .ph-decorative-divider .ph-line {
        flex-grow: 1;
        height: 1px;
        background-color: #c0a062; /* Gold/Brown dotted line color */
        border-top: 1px dashed #c0a062; /* Dashed line effect */
      }

      .ph-decorative-divider .ph-logo-image {
        height: 50px; /* Adjust logo size */
        margin: 0 15px;
      }


      /* Styles for the EXISTING Enquiry Form - UNCHANGED */
      .inquiry-page {
        font-family: "Roboto", sans-serif;
        background-color: #fff;
        color: #333;
        line-height: 1.6;
        /* padding: 20px 5px; -- Original padding, might be overridden by navbar or page layout */
        display: flex;
        flex-direction: column;
        align-items: center;
        min-height: 100vh;
        width: 100%; /* Ensure it takes full width for children like ph-top-bar */
      }

      .form-instructions {
        text-align: center;
        font-size: 0.95em;
        color: #555;
        margin-bottom: 25px;
        min-width: 80%;
        max-width: 800px; /* Added for better control */
      }

      .enquiry-form-container {
        background-color: #fff;
        padding: 30px 40px;
        border-radius: 8px;
        width: 100%;
        max-width: 90%;
      }

      .form-row {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        margin-bottom: 20px;
      }

      .form-group {
        display: flex;
        flex-direction: column;
        flex-grow: 1;
      }

      .form-row.single-column .form-group {
        flex-basis: 100%;
      }
      .form-row.two-columns .form-group {
        flex-basis: calc(50% - 10px);
      }
      .form-row.three-columns .form-group {
        flex-basis: calc(33.333% - 13.33px);
      }

      .form-group label {
        margin-bottom: 6px;
        font-weight: 500;
        color: #333;
        font-size: 0.9em;
      }

      .form-group input[type="text"],
      .form-group input[type="email"],
      .form-group input[type="tel"],
      .form-group input[type="url"],
      .form-group input[type="date"],
      .form-group input[type="number"],
      .form-group select,
      .form-group textarea {
        width: 100%;
        padding: 10px 12px;
        border: 1px solid #ccc;
        border-radius: 6px;
        font-size: 0.95em;
        color: #333;
        background-color: #fff;
      }

      .form-group input:focus,
      .form-group select:focus,
      .form-group textarea:focus {
        outline: none;
        border-color: #888;
        box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.05);
      }

      .form-group textarea {
        resize: vertical;
        min-height: 100px;
      }

      .required {
        color: red;
        font-weight: normal;
        margin-left: 2px;
      }

      .submit-button-container {
        margin-top: 10px;
      }

      .submit-button {
        background-color: #f4a00d;
        color: #f8f8f8;
        padding: 12px 20px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 1em;
        font-weight: 500;
        transition: background-color 0.3s ease;
        display: block;
        width: 100%;
        text-align: center;
      }

      .submit-button:hover {
        background-color: #ed9f0e;
      }

      /* Responsive Adjustments */
      @media (max-width: 768px) {
        .ph-main-title {
            font-size: 1.6em;
        }
        .ph-decorative-divider .ph-logo-image {
            height: 40px;
        }

        .form-row.two-columns .form-group,
        .form-row.three-columns .form-group {
          flex-basis: 100%;
          margin-bottom: 15px;
        }
        .form-row.two-columns .form-group:last-child,
        .form-row.three-columns .form-group:last-child {
          margin-bottom: 0;
        }
        .form-row {
          gap: 0;
          margin-bottom: 0;
        }
        .enquiry-form-container {
          padding: 20px;
          max-width: 95%; /* Allow more width on smaller screens for form */
        }
      }
      @media (max-width: 500px) {
        .ph-main-title {
            font-size: 1.4em;
        }
        .ph-decorative-divider .ph-logo-image {
            height: 35px;
            margin: 0 10px;
        }
        .form-instructions {
          font-size: 0.9em;
          min-width: 90%;
        }
        .form-group label,
        .form-group input,
        .form-group textarea,
        .form-group select,
        .submit-button {
          font-size: 0.9em;
        }
        .enquiry-form-container {
          padding: 20px 10px; /* Less horizontal padding for very small screens */
        }
      }
    </style>
  </head>
  <body>
    <%- include('../partials/navbar'); %>

    <div class="inquiry-page"> <!-- This is the main wrapper from your existing code -->

      <!-- NEW PAGE HEADER SECTION START -->
<br>      
      <div class="ph-title-section">
          <h1 class="ph-main-title">Do You Have any order ?</h1>
          <div class="ph-decorative-divider">
              <span class="ph-line"></span>
              <!-- Replace with your actual logo image -->
              <img src="/public/upload/logo.jpg" alt="Company Logo" class="ph-logo-image">
              <!-- Fallback text logo if image fails or for styling -->
              <!--
              <div class="ph-logo-text">
                <span>LORD AGRO TRADE</span>
                <span class="ph-logo-subtext">CONNECTING FIELDS, EMPOWERING MARKETS</span>
              </div>
              -->
              <span class="ph-line"></span>
          </div>
      </div>
      <!-- NEW PAGE HEADER SECTION END -->

      <p class="form-instructions">
        Please fill in the form below completely. After review, our sales
        associates will contact you.
      </p>

      <div class="enquiry-form-container">
        <form
          id="detailedEnquiryForm"
          action="your-backend-script.php"
          method="POST"
        >
          <!-- Row 1: First Name, Family Name, Country -->
          <div class="form-row three-columns">
            <div class="form-group">
              <label for="firstName"
                >First Name <span class="required">*</span></label
              >
              <input type="text" id="firstName" name="firstName" required />
            </div>
            <div class="form-group">
              <label for="familyName"
                >Family Name <span class="required">*</span></label
              >
              <input type="text" id="familyName" name="familyName" required />
            </div>
            <div class="form-group">
              <label for="country"
                >Country <span class="required">*</span></label
              >
              <input type="text" id="country" name="country" required />
            </div>
          </div>

          <!-- Row 2: Company Name, Company Website -->
          <div class="form-row two-columns">
            <div class="form-group">
              <label for="companyName"
                >Company Name <span class="required">*</span></label
              >
              <input type="text" id="companyName" name="companyName" required />
            </div>
            <div class="form-group">
              <label for="companyWebsite">Company Website</label>
              <input
                type="url"
                id="companyWebsite"
                name="companyWebsite"
                placeholder="https://example.com"
              />
            </div>
          </div>

          <!-- Row 3: Company Address -->
          <div class="form-row single-column">
            <div class="form-group">
              <label for="companyAddress"
                >Company Address <span class="required">*</span></label
              >
              <input
                type="text"
                id="companyAddress"
                name="companyAddress"
                required
              />
            </div>
          </div>

          <!-- Row 4: Phone Call, Email -->
          <div class="form-row two-columns">
            <div class="form-group">
              <label for="phone"
                >Phone Call <span class="required">*</span></label
              >
              <input type="tel" id="phone" name="phone" required />
            </div>
            <div class="form-group">
              <label for="email">Email <span class="required">*</span></label>
              <input type="email" id="email" name="email" required />
            </div>
          </div>

          <!-- Row 5: Product, Amount, Preferred Date -->
          <div class="form-row three-columns">
            <div class="form-group">
              <label for="productName"
                >Name of Product Needed <span class="required">*</span></label
              >
              <input type="text" id="productName" name="productName" required />
            </div>
            <div class="form-group">
              <label for="orderAmount"
                >Amount of Order (kg) <span class="required">*</span></label
              >
              <input type="text" id="orderAmount" name="orderAmount" required />
            </div>
            <div class="form-group">
              <label for="preferredDate"
                >Preferred date to recieve your order
                <span class="required">*</span></label
              >
              <input
                type="date"
                id="preferredDate"
                name="preferredDate"
                required
              />
            </div>
          </div>

          <!-- Row 6: Message -->
          <div class="form-row single-column">
            <div class="form-group">
              <label for="message">Message</label>
              <textarea id="message" name="message" rows="5"></textarea>
            </div>
          </div>

          <!-- Row 7: Submit Button -->
          <div class="form-row single-column submit-button-container">
            <div class="form-group">
              <button type="submit" class="submit-button">Send</button>
            </div>
          </div>
        </form>
      </div>
    </div> <!-- End of .inquiry-page -->

    <%- include('../partials/footer'); %>
    <%- include('../partials/scripts'); %>
  </body>
</html>