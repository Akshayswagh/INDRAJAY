<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>

    <title>Reset Password</title>
    <style>
      :root {
        --reset-pw-primary-color: #f47321;
        --reset-pw-primary-hover: #e0661b;
        --reset-pw-text-color: #333;
        --reset-pw-muted-text: #6c757d;
        --reset-pw-success-color: #28a745;
        --reset-pw-border-color: #ced4da;
        --reset-pw-error-color: #dc3545;
        --reset-pw-background: #f8f9fa;
        --reset-pw-white: #ffffff;
        --reset-pw-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .reset-pw-body {
        background-color: var(--reset-pw-background);
        color: var(--reset-pw-text-color);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .reset-pw-container {
        max-width: 480px;
        width: 100%;
        margin: auto;
        padding: 20px;
      }

      .reset-pw-card {
        background: var(--reset-pw-white);
        border-radius: 10px;
        box-shadow: var(--reset-pw-shadow);
        padding: 2.5rem;
        text-align: center;
      }

      .reset-pw-header {
        margin-bottom: 2rem;
      }

      .reset-pw-icon {
        width: 80px;
        height: 80px;
        margin: 0 auto 1rem;
        background-color: rgba(244, 115, 33, 0.1);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .reset-pw-icon svg {
        width: 36px;
        height: 36px;
        color: var(--reset-pw-primary-color);
      }

      .reset-pw-title {
        font-size: 1.75rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: var(--reset-pw-text-color);
      }

      .reset-pw-subtitle {
        color: var(--reset-pw-muted-text);
        font-size: 1rem;
        margin-bottom: 1.5rem;
      }

      .reset-pw-form-group {
        margin-bottom: 1.5rem;
        text-align: left;
        position: relative;
      }

      .reset-pw-password-wrapper {
        position: relative;
      }

      .reset-pw-form-input {
        width: 100%;
        padding: 1rem;
        border: 1px solid var(--reset-pw-border-color);
        border-radius: 6px;
        font-size: 1rem;
        transition: all 0.2s ease;
        background-color: transparent;
      }

      .reset-pw-form-input:focus {
        outline: none;
        border-color: var(--reset-pw-primary-color);
        box-shadow: 0 0 0 3px rgba(244, 115, 33, 0.2);
      }

      .reset-pw-form-label {
        position: absolute;
        left: 1rem;
        top: 1rem;
        color: var(--reset-pw-muted-text);
        transition: all 0.2s ease;
        pointer-events: none;
        background-color: var(--reset-pw-white);
        padding: 0 0.25rem;
      }

      .reset-pw-form-input:focus + .reset-pw-form-label,
      .reset-pw-form-input:not(:placeholder-shown) + .reset-pw-form-label {
        top: -0.6rem;
        font-size: 0.75rem;
        color: var(--reset-pw-primary-color);
      }

      .reset-pw-password-toggle {
        position: absolute;
        right: 1rem;
        top: 50%;
        transform: translateY(-50%);
        cursor: pointer;
        color: var(--reset-pw-muted-text);
      }

      .reset-pw-password-toggle:hover {
        color: var(--reset-pw-text-color);
      }

      .reset-pw-form-text {
        display: block;
        margin-top: 0.5rem;
        font-size: 0.75rem;
        color: var(--reset-pw-muted-text);
      }

      .reset-pw-submit-button {
        width: 100%;
        padding: 1rem;
        background-color: var(--reset-pw-primary-color);
        color: var(--reset-pw-white);
        border: none;
        border-radius: 6px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background-color 0.2s ease;
      }

      .reset-pw-submit-button:hover {
        background-color: var(--reset-pw-primary-hover);
      }

      .reset-pw-submit-button:disabled {
        background-color: var(--reset-pw-muted-text);
        cursor: not-allowed;
      }

      .reset-pw-message-box {
        padding: 1rem;
        border-radius: 6px;
        margin-top: 1.5rem;
        font-size: 0.875rem;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
      }

      .reset-pw-error-message {
        background-color: rgba(220, 53, 69, 0.1);
        color: var(--reset-pw-error-color);
        border: 1px solid rgba(220, 53, 69, 0.2);
      }

      .reset-pw-success-message {
        background-color: rgba(40, 167, 69, 0.1);
        color: var(--reset-pw-success-color);
        border: 1px solid rgba(40, 167, 69, 0.2);
      }

      .reset-pw-loading {
        position: relative;
        pointer-events: none;
        color: transparent;
      }

      .reset-pw-loading::after {
        content: "";
        position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
        width: 20px;
        height: 20px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-radius: 50%;
        border-top-color: white;
        animation: reset-pw-spin 1s linear infinite;
      }

      @keyframes reset-pw-spin {
        to {
          transform: translate(-50%, -50%) rotate(360deg);
        }
      }

      @media (max-width: 480px) {
        .reset-pw-container {
          padding: 1rem;
        }

        .reset-pw-card {
          padding: 1.5rem;
        }

        .reset-pw-icon {
          width: 72px;
          height: 72px;
        }
      }
    </style>
  </head>
  <body >
    <%- include('../partials/navbar'); %>
    <div class="reset-pw-container">
      <div class="reset-pw-card">
        <% if (valid) { %>
        <div class="reset-pw-header">
          <div class="reset-pw-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
              <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
            </svg>
          </div>
          <h1 class="reset-pw-title">Set New Password</h1>
          <p class="reset-pw-subtitle">Create a strong, secure password</p>
        </div>

        <form id="resetPasswordForm">
          <div class="reset-pw-form-group">
            <div class="reset-pw-password-wrapper">
              <input
                type="password"
                id="reset-pw-password"
                name="password"
                class="reset-pw-form-input"
                placeholder=" "
                required
                minlength="8"
                pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$"
                title="Password must contain at least 8 characters, including uppercase, lowercase and number"
              />
              <label for="reset-pw-password" class="reset-pw-form-label">New Password</label>
              <span class="reset-pw-password-toggle">
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="20"
                  height="20"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
                  <circle cx="12" cy="12" r="3"></circle>
                </svg>
              </span>
            </div>
            <small class="reset-pw-form-text"
              >8+ characters with uppercase, lowercase and number</small
            >
          </div>

          <button type="submit" class="reset-pw-submit-button" id="reset-pw-submit-button">
            Reset Password
          </button>
        </form>

        <div
          id="reset-pw-response-message"
          class="reset-pw-message-box"
          role="alert"
          style="display: none"
        ></div>
        <% } else { %>
        <div class="reset-pw-message-box reset-pw-error-message">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="12" y1="8" x2="12" y2="12"></line>
            <line x1="12" y1="16" x2="12.01" y2="16"></line>
          </svg>
          Invalid or expired password reset link. Please request a new one.
        </div>
        <% } %>
      </div>
    </div>



    <%- include('../partials/footer'); %>
    <%- include('../partials/scripts'); %>
     <% if (valid) { %>
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        // Password toggle functionality
        const passwordToggle = document.querySelector(".reset-pw-password-toggle");
        const passwordInput = document.getElementById("reset-pw-password");

        if (passwordToggle && passwordInput) {
          passwordToggle.addEventListener("click", function () {
            const type =
              passwordInput.getAttribute("type") === "password"
                ? "text"
                : "password";
            passwordInput.setAttribute("type", type);

            // Toggle SVG icon
            const svg = this.querySelector("svg");
            if (type === "text") {
              svg.innerHTML = `
              <path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path>
              <line x1="1" y1="1" x2="23" y2="23"></line>
            `;
            } else {
              svg.innerHTML = `
              <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
              <circle cx="12" cy="12" r="3"></circle>
            `;
            }
          });
        }

        // Form submission
        const form = document.getElementById("resetPasswordForm");
        const submitButton = document.getElementById("reset-pw-submit-button");
        const responseMessage = document.getElementById("reset-pw-response-message");

        form.addEventListener("submit", async (e) => {
          e.preventDefault();

          // Validate password pattern
          const password = form.password.value;
          const passwordPattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/;
          if (!passwordPattern.test(password)) {
            responseMessage.innerHTML = `
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="12" y1="8" x2="12" y2="12"></line>
              <line x1="12" y1="16" x2="12.01" y2="16"></line>
            </svg>
            Password must contain at least 8 characters, including uppercase, lowercase and number
          `;
            responseMessage.className = "reset-pw-message-box reset-pw-error-message";
            responseMessage.style.display = "flex";
            return;
          }

          try {
            // Show loading state
            submitButton.disabled = true;
            submitButton.classList.add("reset-pw-loading");
            responseMessage.style.display = "none";

            const response = await fetch(
              `/api/auth/reset-password/<%= token %>`,
              {
                method: "POST",
                headers: {
                  "Content-Type": "application/json",
                },
                body: JSON.stringify({
                  password: password,
                }),
              }
            );

            const data = await response.json();

            if (response.ok) {
              responseMessage.innerHTML = `
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                <polyline points="22 4 12 14.01 9 11.01"></polyline>
              </svg>
              ${data.message || "Password reset successfully!"}
            `;
              responseMessage.className = "reset-pw-message-box reset-pw-success-message";
              responseMessage.style.display = "flex";

              setTimeout(() => {
                window.location.href = "/login-admin";
              }, 2000);
            } else {
              responseMessage.innerHTML = `
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <line x1="12" y1="8" x2="12" y2="12"></line>
                <line x1="12" y1="16" x2="12.01" y2="16"></line>
              </svg>
              ${data.message || "Error resetting password"}
            `;
              responseMessage.className = "reset-pw-message-box reset-pw-error-message";
              responseMessage.style.display = "flex";
            }
          } catch (error) {
            console.error("Error:", error);
            responseMessage.innerHTML = `
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="12" y1="8" x2="12" y2="12"></line>
              <line x1="12" y1="16" x2="12.01" y2="16"></line>
            </svg>
            Network error. Please check your connection and try again.
          `;
            responseMessage.className = "reset-pw-message-box reset-pw-error-message";
            responseMessage.style.display = "flex";
          } finally {
            submitButton.disabled = false;
            submitButton.classList.remove("reset-pw-loading");
          }
        });
      });
    </script>
    <% } %>
  </body>
</html>